/*using System.Collections.Generic;
using System.Text;
using Microsoft.Xna.Framework;
using Terraria;
using Terraria.ID;
using Terraria.ModLoader;
using Terraria.UI;
// If you are using c# 6, you can use: "using static Terraria.Localization.GameCulture;" which would mean you could just write "DisplayName.AddTranslation(German, "");"
//using Terraria.Localization;

namespace TerrariaReworked.Projectiles
{
	// this gets executed not only for coal, byt for all items
	/*public class VanillaProjectileChanger : GlobalProjectile
	{/*
		public override void PostAI( Projectile projectile )
		{
			StringBuilder sb = new StringBuilder();
			for( int i = 0; i < Main.dust.Length; i++ )
			{
				if( Main.dust[i] != null && Main.dust[i].type != 0 )
				{
					sb.Append( Main.dust[i].type + ", " );
				}
			}
		Main.NewText( sb.ToString(), new Color( 0, 255, 0 ) );
		}*
	}
}*/